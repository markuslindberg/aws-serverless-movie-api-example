{
  "name": "movieapi",
  "version": "1.0.0",
  "author": "Markus Lindberg",
  "license": "MIT",
  "scripts": {
    "test": "cd test/MovieApi.Tests && dotnet test --collect:\"XPlat Code Coverage\" --settings coverlet.runsettings",
    "build": "sam build --parallel --cached",
    "build:prod": "sam build --parallel --cached --config-env prod",
    "deploy": "sam deploy",
    "deploy:prod": "sam deploy --config-env prod",
    "delete": "sam delete",
    "delete:prod": "sam delete --config-env prod",
    "sync": "sam sync"
  }
}